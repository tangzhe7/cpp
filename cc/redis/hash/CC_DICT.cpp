#include "./CC_DICT.h"
#include "../../env/CCENV.h"
namespace cc
{

	/*template<class K, class V> CC_DICT<K,V>::~CC_DICT() 
	{
		
	};*/
	/*template<class K, class V> CC_DICT<K, V>::CC_DICT()
	{
		this->trehashidx = -1;
		this->hascode = CC_NULL;
	};
	template<class K, class V> CC_DICT<K, V>::CC_DICT(U_CC_INT32(*hashcode)(const K& key))
	{
		
	};
	template<class K, class V> void CC_DICT<K, V>::setHashFunc(U_CC_INT32(*hashcode)(const K& key))
	{
	
	};*/
	/**
	 *如果存在直接返回
	 *
	 **/
	/*void put(K k, V v);
	const K& operator[]();
	V get(K k);*/

}